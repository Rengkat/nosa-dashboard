import React from "react";

const SubHeading = ({ text }) => {
  return (
    <div className="bg-gray-200 p-5 shadow text-xl text-gray-600 font-bold rounded">{text}</div>
  );
};

export default SubHeading;
